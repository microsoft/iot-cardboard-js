/** File for exporting common testing utilities for the context */

import { IOatPageContextState } from './OatPageContext.types';

export const GET_MOCK_DEEPLINK_STATE = (): IOatPageContextState => ({
    adtUrl: 'https://myurl.adt-0'
});

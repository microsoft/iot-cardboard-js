export enum Theme {
    Light = 'light',
    Dark = 'dark'
}

export enum TSIComponentTypes {
    linechart = 'linechart',
    barchart = 'barchart'
}

export enum Locale {
    EN = 'en',
    DE = 'de'
}

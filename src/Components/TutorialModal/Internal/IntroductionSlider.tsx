import React from 'react';

const IntroductionSlider: React.FC<{
    slideNumber: number;
    setSliderNumber: (slideNumber: number) => void;
}> = () => {
    return <div></div>;
};

export default React.memo(IntroductionSlider);

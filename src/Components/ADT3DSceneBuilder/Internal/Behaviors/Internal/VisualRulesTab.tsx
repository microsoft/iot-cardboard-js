import React from 'react';
import { VisualRulesList } from '../VisualRules/VisualRuleList';

export const VisualRulesTab: React.FC<any> = (_props: any) => {
    return <VisualRulesList ruleItems={[]} />;
};

{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "chrome",
            "request": "launch",
            "name": "Storybook Debug - Launch Chrome against localhost",
            "url": "http://localhost:6006", // change the port based on where your storybook is running if necessary
            "webRoot": "${workspaceFolder}"
        }
    ]
}
